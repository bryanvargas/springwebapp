<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:jee="http://www.springframework.org/schema/jee"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-4.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.2.xsd">


<!-- 	//esto nod indica que nuestra base de datos va a ser accesada(conextarse) -->
<!-- 	//mediante JDNI y no con JDBC -->
	<jee:jndi-lookup jndi-name="jdbc/spring" id="dataSource"
		expected-type="javax.sql.DataSource">
	</jee:jndi-lookup>

	<!-- esto nos permite el uso de anotaciones habilitando las anotaciones
	 de inyeccion de dependnecias d spring-->
	<context:annotation-config></context:annotation-config>
	
	
	<!-- esta nos va a escanear el paquete shingo.dao en busca de anotaciones 
		spring -->
	<!-- referentes a la inyection -->
	<context:component-scan base-package="com.shingo.dao"></context:component-scan>
	
<!-- 	creamos la fabrica de sessiones hibernate, dado que hibernate trabaja con seciones y le asignamos una clase -->
<!-- 	y serie de propiedades 
		La primera oropiedad hace referencia a dataSource para que se conecte a la base de datos.
		packageToScan va a hacer referencia a las clases que va a tener el mapping relacionados al objetos
		y luego tenemos propiedades especificas a hibernate que primero es el
		DIalecto que sirve para poder trabajar con mysql y
		ShowSQL para que nos muestre por consola las sentencias sql que va a hacer hibernate-->
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
		<property name="dataSource" ref="dataSource"></property>
		<property name="packagesToScan" value="com.shingo.pojo"></property>
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">org.hibernate.dialect.MySQL5Dialect</prop>
				<prop key="hibernate.show_sql">true</prop>
			</props>
		</property>
	</bean>

	<!-- 	Este bean lo que va a hacer es traducirnos las excepciones hibernate a spring -->
	<bean id="exceptionTranslator"
		class="org.springframework.dao.annotation.PersistenceExceptionTranslationPostProcessor">
	</bean>
	
<!-- 	Y este bean nos servira para habilitar transacciones en hibernate -->
	<bean id="transactionManager"
		class="org.springframework.orm.hibernate5.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory"></property>
	</bean>

	<!-- 	y annotacion-driven, que nos sirve para habilitar las anotraciones de transactioalidad haciendo referencia a -->
<!-- 	transaction manager -->
	<tx:annotation-driven transaction-manager="transactionManager" />
</beans>
